export MASSPRED_INPUT="${INPUT}"
export MASSPRED_INPUT_POSITION="${INPUT_POSITION}"
export MASSPRED_OUTPUT="${OUTPUT}"
export MASSPRED_FASTA_DB="${FASTA_DB}"
export MASSPRED_FASTA_ID="${FASTA_ID}"
export MASSPRED_FASTA_REFERENCE_DB="${FASTA_REFERENCE_DB}"
export MASSPRED_FASTA_REFERENCE="${FASTA_REFERENCE}"
export MASSPRED_FILE_NAME="${FILE_NAME}"

PATH="${PATH}:$(dirname $(readlink -f ${CONFIGURATION}))"

${COMMAND} < ${TMP}/in 1> ${OUTPUT}.out 2> ${OUTPUT}.err
echo ${?} > ${OUTPUT}.rc
